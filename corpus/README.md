# Custom RAG corpus (Formula One)

Thirteen markdown notes used for the Assignment 7 custom RAG app (HANS, 2022 ground effect, porpoising, Hamilton titles, undercut/overcut, flags, …).

Index with the official papers:

```bash
uv run python build_index.py --reset
```

See `CORPUS_MANIFEST.md` for per-file chunk counts (51 total) and `custom_queries.py` for the five queries.
